### Steps
1. Edit `app/src/main/java/dev/aurakai/auraframefx/cascade/trinity/TrinityRepository.kt` — update `getThemes()` to emit `Result<List<Theme>>` by calling `mapToDomainTheme()` for each network theme (`response.map { mapToDomainTheme(it) }`) and ensure `applyTheme()` emits `Result<Theme>` using `mapToDomainTheme(response)`.
2. In the same file, replace `mapToUserData(networkUser: NetworkUser)` implementation to construct domain `UserData` using only existing network fields: `id`, `username`, `email` (use named params: `UserData(id = ..., name = ..., email = ...)`) and remove any references to `profileImageUrl`, `agentType`, `isActive`, `lastActivity`.
3. Update `mapToDomainAgentStatus(agentResponse: AgentResponse)` to map only from existing `AgentResponse` fields (e.g., `agentName`, `confidence`, `timestamp`, `error`, `metadata`). Provide safe defaults (e.g., `"unknown"` for id, `AgentStatus.Status.IDLE` if confidence absent/low, `isAvailable = error == null`) and avoid referencing any non-existent fields.
4. Search the codebase for other mappers or call sites that expect network models (search for `NetworkTheme`, `NetworkUser`, `profileImageUrl`, `agentType`, `isActive`, `lastActivity`) and update them to use the domain mappers or remove invalid property access.
5. After changes, run a clean build and compile to confirm the type-system errors related to these mappings are resolved before moving onto Phase 4.

### Further Considerations
1. Confirm `AgentResponse` shape: Option A — it includes `confidence` (float) → keep current confidence-based status mapping; Option B — it has explicit `status` string → map that string to `AgentStatus.Status` (ACTIVE/IDLE/etc.). Which applies in your `network.model.AgentResponse`?
2. If `applyTheme()` is declared `suspend fun` but returns `flow { ... }`, consider whether it should be `fun applyTheme(...) = flow<Result<Theme>>` (no `suspend`) — do you want it suspend or return a cold Flow? (I can propose the exact signature once you confirm preferred behavior.)
3. After these edits, I can prepare a small follow-up plan to address the remaining Phase 4 type errors (GenesisAgent ViewModel overloads, Flow collection issues, SecurityEvent property errors) — would you like me to continue to that next?
